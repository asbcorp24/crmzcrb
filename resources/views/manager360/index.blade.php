@extends('layouts.app')
@section('title','360° руководителя — CRM ЗЦРБ')
@section('header','360° руководителя')
@section('content')
<div class="row g-3 mb-4"><div class="col-6 col-lg-3"><div class="card stat-card h-100"><div class="card-body"><div class="text-muted small">Сотрудников в ветке</div><div class="display-6 fw-semibold">{{ $users->count() }}</div></div></div></div><div class="col-6 col-lg-3"><div class="card stat-card h-100"><div class="card-body"><div class="text-muted small">Активных задач</div><div class="display-6 fw-semibold">{{ $tasks->count() }}</div></div></div></div><div class="col-6 col-lg-3"><div class="card stat-card h-100"><div class="card-body"><div class="text-muted small">Просрочено</div><div class="display-6 fw-semibold text-danger">{{ $tasks->filter(fn($t)=>$t->is_overdue)->count() }}</div></div></div></div><div class="col-6 col-lg-3"><div class="card stat-card h-100"><div class="card-body"><div class="text-muted small">Отсутствуют сегодня</div><div class="display-6 fw-semibold">{{ $absentCount }}</div></div></div></div></div>
<div class="row g-4"><div class="col-xl-8"><div class="card border-0 shadow-sm"><div class="card-header bg-white py-3"><h5 class="mb-0">Нагрузка сотрудников</h5><div class="small text-muted">Индекс учитывает активные, критические, просроченные и ближайшие задачи.</div></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Сотрудник</th><th>Активные</th><th>Критические</th><th>Просрочено</th><th>До 3 дней</th><th>Нагрузка</th></tr></thead><tbody>@foreach($loads as $x)<tr><td><a class="fw-semibold text-decoration-none" href="{{ route('employees.profile',$x['user']) }}">{{ $x['user']->full_name }}</a><div class="small text-muted">{{ $x['user']->department?->name }} · {{ $x['user']->position }}</div>@if($x['absent'])<span class="badge text-bg-warning">Отсутствует</span>@endif</td><td>{{ $x['active'] }}</td><td class="{{ $x['critical']?'text-danger fw-semibold':'' }}">{{ $x['critical'] }}</td><td class="{{ $x['overdue']?'text-danger fw-semibold':'' }}">{{ $x['overdue'] }}</td><td>{{ $x['soon'] }}</td><td><div class="d-flex align-items-center gap-2"><div class="progress flex-grow-1" style="min-width:90px;height:8px"><div class="progress-bar" style="width:{{ min(100,$x['score']*5) }}%"></div></div><b>{{ $x['score'] }}</b></div></td></tr>@endforeach</tbody></table></div></div></div><div class="col-xl-4"><div class="card border-0 shadow-sm"><div class="card-header bg-white py-3"><h5 class="mb-0">Планы под риском</h5><div class="small text-muted">До окончания ≤ 7 дней, прогресс ниже 80%.</div></div><div class="list-group list-group-flush">@forelse($lagging as $p)<a href="{{ route('plans.page',['user_id'=>$p->user_id]) }}" class="list-group-item list-group-item-action"><div class="fw-semibold">{{ $p->title }}</div><div class="small text-muted">{{ $p->user?->full_name }} · до {{ $p->period_end?->format('d.m.Y') }}</div><div class="progress mt-2" style="height:7px"><div class="progress-bar" style="width:{{ $p->progress }}%"></div></div><div class="small mt-1">{{ $p->progress }}%</div></a>@empty<div class="p-3 text-muted">Рисковых планов сейчас нет.</div>@endforelse</div></div></div></div>
@endsection
